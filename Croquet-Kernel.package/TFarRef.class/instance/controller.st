accessing
controller

	"pvtController may answer a FarRef to the currently active island, which is incorrect; sync sending yourself will resolve the invalid FarRef if so, and do nothing if not."
	^self pvtController yourself