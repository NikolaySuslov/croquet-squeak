accessing
isPremultiplied: bFlag

	"Specify whether this texture contains premultiplied color values
	 (usually as a result of supersampling without normalization,
      such as in a dynamically generated image). 

	 Default is false."

	isPremultiplied := bFlag