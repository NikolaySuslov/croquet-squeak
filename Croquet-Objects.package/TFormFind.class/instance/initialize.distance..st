initialize
initialize:thumb distance: dist

	sha := thumb sha.
	url := thumb url.
	bMipmap := thumb bMipmap.
	bShrinkFit := thumb bShrinkFit.
	extension := thumb extension.
	distance := dist.
	" routerAddress := ? "
	" routerID := ? "
	
